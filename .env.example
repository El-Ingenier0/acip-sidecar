# MoltBot ACIP Sidecar env
# Never commit real keys. Copy to .env (gitignored).

# Gemini (Level 1)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Anthropic (Level 2 fallback)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-latest

# Server
ACIP_BIND=127.0.0.1
ACIP_PORT=18795
